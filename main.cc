#include "Controller.h"
#include <iostream>
#include <stack>

// bool StrContainsX(const std::string& str) {
//     return (str.find('x') == std::string::npos) ? false : true;
// }

// // class Solution {
// // public:
// //     bool isValid(string s) {
// //         std::stack<char> st;
// //         if ((s.length()&1)) return false;
// //         for(const auto& var: s) {
// //             if (var == '(') st.push(')');
// //             else if (var == '[') st.push(']');
// //             else if (var == '{') st.push('}');
// //             else {
// //                 if (var != st.top()) return false;
// //                 else st.pop();
// //             }
// //         }
// //         return true;
// //     }
// // };

int main() {

    Controller qwe;
    // std::cout<<qwe.GetRes("2x","3").second;
    // std::string asd = "1e-3 + 0.1 + 0.1";
    // std::cout<<qwe.GetRes(asd,"").second;
    // std::cout<<qwe.CheckIfInputStringIsOk(asd);

    // Solution asd;
    std::string z = "((";
    // std::cout<<asd.isValid("");
}
